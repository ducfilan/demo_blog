<%= provide(:title, t("titles.index")) %>
<div class="row">
	<div class="col-md-12">
		<table class="table table-default">
			<% @users.each do |user| %>
				<%= render user %>
			<% end %>
			<%= will_paginate %>
		</table>
	</div>
</div>
